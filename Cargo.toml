[package]
name = "rime-poc"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
anyhow = "1.0"
rime-api = "0.12.2"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"

[build-dependencies]
cc = "1.0"
