# pinyin v0.1.0

pinyin 的首个公开版本。

## 核心体验

- 保持 macOS 系统英文输入源，通过默认前缀 `''` 临时进入中文拼音输入；
- 再次输入 `''` 退出，无需切换系统输入法；
- 使用本机 Rime 候选，支持空格上屏、数字选词和候选翻页；
- 支持逐段转换、中文标点、英文原样提交和实验性 Rime 自动混输；
- 在切换应用、窗口或输入源时清理会话，避免输入状态跨上下文泄漏。

## 下载

Release 提供两个自包含的 macOS 便携包：

- `pinyin-v0.1.0-macos-arm64.zip`：Apple Silicon；
- `pinyin-v0.1.0-macos-x86_64.zip`：Intel Mac。

下载后可使用 `SHA256SUMS` 校验文件完整性。便携包包含 librime、运行时动态库、基础 Rime 数据和第三方许可证材料，不要求目标 Mac 安装 Homebrew。

## 系统要求与限制

- 仅支持 macOS；
- 必须手动授予“辅助功能”和“输入监控”权限；
- 当前通过终端常驻运行，尚无菜单栏界面、自动启动或自动更新；
- 当前构建使用临时签名，尚未完成 Apple Developer ID 签名与公证；
- 只在 macOS 系统输入源下开启会话，第三方输入法不会触发 pinyin。

首次使用和配置方式请参阅项目 [README](README.md)。
